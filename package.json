{
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon node server/index.js",
    "build": "cd client && npm install && npm run build",
    "ui": "cd client && npm start"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "nodemon": "^2.0.16"
  }
}
